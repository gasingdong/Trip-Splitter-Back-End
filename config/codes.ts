const BAD_REQUEST = { code: 'BadRequest' };

const ID_CONFLICT = { code: 'IdConflict' };

const INVALID_CRED = { code: 'InvalidCredentials' };

const NOT_FOUND = { code: 'NotFound' };

const SERVER_ERR = { code: 'UnexpectedServerError' };

export default {
  BAD_REQUEST,
  ID_CONFLICT,
  INVALID_CRED,
  SERVER_ERR,
  NOT_FOUND,
};
